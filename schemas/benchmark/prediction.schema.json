{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Problem Contract Benchmark review prediction",
  "$defs": {
    "field_review": {
      "type": "object",
      "additionalProperties": false,
      "required": ["verdict", "rationale", "evidence_refs"],
      "properties": {
        "verdict": {"enum": ["pass", "minor_issue", "major_issue"]},
        "rationale": {"type": "string", "minLength": 1},
        "evidence_refs": {
          "type": "array",
          "items": {"type": "string", "minLength": 1}
        }
      }
    },
    "must_fix": {
      "type": "object",
      "additionalProperties": false,
      "required": ["field", "issue"],
      "properties": {
        "field": {
          "enum": [
            "schema_version",
            "problem_id",
            "parent_problem_id",
            "subproblem_ids",
            "title",
            "abstract",
            "background",
            "references",
            "previous_progress",
            "problem_statement",
            "scientific_significance",
            "solution_difficulty",
            "verification_contract",
            "verification_difficulty",
            "scientific_solidity",
            "cross_field_consistency",
            "evidence_fidelity",
            "scope_assessment",
            "resolution_gate"
          ]
        },
        "issue": {"type": "string", "minLength": 1}
      }
    }
  },
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "case_id",
    "problem_id",
    "field_reviews",
    "scope_assessment",
    "resolution_gate",
    "overall_verdict",
    "overall_rationale",
    "must_fix",
    "rewrite_prompt"
  ],
  "properties": {
    "schema_version": {"type": "integer", "const": 2},
    "case_id": {"type": "string", "pattern": "^ORCB-[A-F0-9]{12}$"},
    "problem_id": {"type": "string", "minLength": 1},
    "field_reviews": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "schema_version",
        "problem_id",
        "parent_problem_id",
        "subproblem_ids",
        "title",
        "abstract",
        "background",
        "references",
        "previous_progress",
        "problem_statement",
        "scientific_significance",
        "solution_difficulty",
        "verification_contract",
        "verification_difficulty",
        "scientific_solidity",
        "cross_field_consistency",
        "evidence_fidelity"
      ],
      "properties": {
        "schema_version": {"$ref": "#/$defs/field_review"},
        "problem_id": {"$ref": "#/$defs/field_review"},
        "parent_problem_id": {"$ref": "#/$defs/field_review"},
        "subproblem_ids": {"$ref": "#/$defs/field_review"},
        "title": {"$ref": "#/$defs/field_review"},
        "abstract": {"$ref": "#/$defs/field_review"},
        "background": {"$ref": "#/$defs/field_review"},
        "references": {"$ref": "#/$defs/field_review"},
        "previous_progress": {"$ref": "#/$defs/field_review"},
        "problem_statement": {"$ref": "#/$defs/field_review"},
        "scientific_significance": {"$ref": "#/$defs/field_review"},
        "solution_difficulty": {"$ref": "#/$defs/field_review"},
        "verification_contract": {"$ref": "#/$defs/field_review"},
        "verification_difficulty": {"$ref": "#/$defs/field_review"},
        "scientific_solidity": {"$ref": "#/$defs/field_review"},
        "cross_field_consistency": {"$ref": "#/$defs/field_review"},
        "evidence_fidelity": {"$ref": "#/$defs/field_review"}
      }
    },
    "scope_assessment": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "impact",
        "impact_rationale",
        "scope_verdict",
        "generalization_action",
        "unnecessary_restrictions",
        "rationale"
      ],
      "properties": {
        "impact": {"enum": ["high", "medium", "low"]},
        "impact_rationale": {"type": "string", "minLength": 1},
        "scope_verdict": {
          "enum": [
            "appropriate",
            "unnecessarily_narrow",
            "too_broad",
            "source_misaligned"
          ]
        },
        "generalization_action": {
          "enum": ["keep", "broaden", "narrow", "decompose"]
        },
        "unnecessary_restrictions": {
          "type": "array",
          "items": {"type": "string", "minLength": 1}
        },
        "rationale": {"type": "string", "minLength": 1}
      }
    },
    "resolution_gate": {
      "type": "object",
      "additionalProperties": false,
      "required": ["status", "rationale"],
      "properties": {
        "status": {"enum": ["pass", "fail", "delegated_parent"]},
        "rationale": {"type": "string", "minLength": 1}
      }
    },
    "overall_verdict": {"enum": ["accept", "rewrite", "reject"]},
    "overall_rationale": {"type": "string", "minLength": 1},
    "must_fix": {"type": "array", "items": {"$ref": "#/$defs/must_fix"}},
    "rewrite_prompt": {"type": "string"}
  }
}
