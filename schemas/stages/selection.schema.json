{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Selection Agent output",
  "description": "Merged canonicalization and routing for one topic. The Selection Agent reads the topic's memory.md (source records collected by Discovery) and returns the canonical candidates worth the expensive later-literature Research audit. Each candidate carries its source-faithful canonical formulation, an importance routing level, and a free-form assessment the pipeline appends to the candidate's memory.md as context for the Research Agent. It does not produce the problem's verification contract: the verification contract, verification difficulty, and CI contracts are produced by the Research Agent (see schemas/problem.schema.json).",
  "type": "object",
  "additionalProperties": false,
  "required": ["candidates"],
  "properties": {
    "candidates": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "canonical_title",
          "canonical_statement",
          "domain",
          "source_keys",
          "source_support",
          "importance_level",
          "assessment"
        ],
        "properties": {
          "canonical_title": {"type": "string", "minLength": 1},
          "canonical_statement": {
            "description": "Source-faithful statement preserving the literature problem's natural generality. Famous problems must align with an authoritative formulation; restricted variants must be labeled as derived in the assessment.",
            "type": "string",
            "minLength": 1
          },
          "domain": {"type": "string", "minLength": 1},
          "source_keys": {
            "type": "array",
            "minItems": 1,
            "items": {"type": "string"}
          },
          "source_support": {
            "description": "Per-source supporting excerpts: exactly one entry per source_key, each a verbatim substring of that source record's text.",
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": ["source_key", "exact_excerpt"],
              "properties": {
                "source_key": {"type": "string", "minLength": 1},
                "exact_excerpt": {"type": "string", "minLength": 1}
              }
            }
          },
          "importance_level": {
            "description": "Only high or medium importance proceeds to the expensive later-literature Research audit, so this is a real gate with downstream consequences, not a decorative label.",
            "enum": ["high", "medium", "low", "unassessed"]
          },
          "assessment": {
            "description": "Free-form screening narrative appended to the candidate's memory.md for the Research Agent: why the candidate matters, what solving it would change, and any scope or named-problem concerns (including alignment with an authoritative formulation). This is context for the next stage, not a machine-consumed contract.",
            "type": "string",
            "minLength": 1
          }
        }
      }
    }
  }
}
