# Baseline

Store the current best construction, bound, algorithm, or certificate needed to
judge progress. Every baseline must point to an authoritative source and state
the domain, assumptions, and exact comparison metric.
